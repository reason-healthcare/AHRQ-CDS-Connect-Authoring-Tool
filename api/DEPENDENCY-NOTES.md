# Dependency Notes

TODO Preston Lee Update from CommonJS-to-ECMAScript Module upgrade

Where possible, dependencies are frequently updated to their latest versions.
In some cases, however, this is not feasible or does not provide sufficient
return on the necessary investment to do a major version upgrade. The following
dependencies are currently fixed to older versions:

The CQL grammar files were generated as follows:

- v1.3 generated by ANTLR 4.8.0 and no longer used
- v1.5.3 generated by ANTLR 4.13.2 with `antlr -Dlanguage=JavaScript -visitor cql.g4`

**IMPORTANT**: ANTLR4 versions after 4.8.0 generate code that uses ESM style imports instead of Common JS requires statements. Since the JavaScript runtime must match the version of ANTLR4 used to generate the classes, we are only using the the antlr4 JavaScript from 4.13.2 and later.
